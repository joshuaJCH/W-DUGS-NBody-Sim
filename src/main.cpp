#include <iostream>

// =========================================================
// NOTES:
// - run 'main' for the simulation
// - run 'tests' for testing (the other things named "Catch/Catch2WithMain" are junk, just ignore)
// =========================================================



int main() {
    std::cout << "W-DUGS Simulation Ready" << std::endl;
    return 0;
}